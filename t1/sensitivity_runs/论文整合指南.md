# 敏感性分析论文整合指南

## 一、快速总结

**核心结论**：模型对关键超参数（`alpha0`和`kappa`）的选择**不敏感**，在广泛的参数范围内都能保持：
- ✅ **一致性极强**：`consistency_map=1.0000`（100%）在所有参数组合下都成立
- ✅ **不确定性可控**：可以通过调整`kappa`来平衡不确定性和时间序列平滑度
- ✅ **计算效率稳定**：接受率在所有参数组合下都稳定在18-21%之间

---

## 二、论文中的位置和内容

### 2.1 方法部分（简要提及）

**位置**：在介绍ABC方法和参数设置后（约2-3句话）

**建议内容**：

> 为了评估模型对关键超参数的稳健性，我们进行了敏感性分析。我们系统地变化了Dirichlet先验的初始浓度参数（`alpha0` ∈ {1.0, 5.0, 20.0}）和动态先验的平滑强度系数（`kappa` ∈ {10.0, 40.0, 120.0}），共9组参数组合。所有敏感性分析使用相同的固定参数（`n_accept=500`, `draw_max=50000`），并评估了模型的一致性、不确定性和计算效率。

### 2.2 结果部分（详细报告）

**位置**：在主结果报告后，添加"敏感性分析"小节（约1-2段）

**建议内容**：

> **敏感性分析结果**：我们评估了模型对关键超参数（`alpha0`和`kappa`）的稳健性。在所有9组参数组合中，基于MAP估计的一致性（`consistency_map`）均为1.0000（100%），表明模型对参数选择不敏感。基于后验均值的一致性（`consistency_mean`）在大多数参数组合下为1.0000，最低为0.9967（300/301周正确）。相对置信区间宽度的中位数随`kappa`增加而减小（从2.3-2.6降至0.7-0.8），而`alpha0`对不确定性的影响较小。接受率在所有参数组合下都稳定在18-21%之间，表明计算效率不受参数选择影响。这些结果表明，模型在广泛的参数范围内都能保持高一致性和合理的计算效率。

**关键数据点**：
- `consistency_map`：所有组合均为1.0000
- `consistency_mean`：大多数为1.0000，最低为0.9967
- 相对CI宽度：随`kappa`从2.3-2.6降至0.7-0.8
- 接受率：稳定在18-21%之间

### 2.3 附录/补充材料（详细结果）

**位置**：论文附录或补充材料部分

**建议内容**：

1. **Table S1**：敏感性分析结果汇总表
   - 文件：`sensitivity_outputs/table_S1.csv` 或 `table_S1.md`
   - 包含：alpha0, kappa, consistency_map, consistency_mean, accept_rate_median, rel_CI_width_median, margin_median

2. **Figure S1**：Consistency vs kappa（按alpha0分组）
   - 文件：`sensitivity_outputs/fig_S1_consistency_vs_kappa.png`
   - 说明：展示`kappa`对一致性的影响，以及`alpha0`的影响较小

3. **Figure S2**：Relative CI Width vs kappa（按alpha0分组）
   - 文件：`sensitivity_outputs/fig_S2_rel_ci_vs_kappa.png`
   - 说明：展示`kappa`对不确定性的显著影响，以及`alpha0`的影响较小

---

## 三、关键结论的强调

### 3.1 在论文中应强调的要点

1. ✅ **稳健性极强**：
   - 模型在广泛的参数范围内都能保持高一致性
   - `consistency_map=1.0000`在所有参数组合下都成立
   - 这证明了模型对参数选择不敏感

2. ✅ **参数选择不敏感**：
   - `alpha0`和`kappa`的选择对主要结果（一致性）影响很小
   - 默认参数（`alpha0=5.0, kappa=40.0`）的选择是合理的

3. ✅ **不确定性可控**：
   - 可以通过调整`kappa`来平衡不确定性和时间序列平滑度
   - 更大的`kappa`带来更小的不确定性，但可能过度平滑时间序列变化

4. ✅ **计算效率稳定**：
   - 接受率在所有参数组合下都稳定在合理范围内
   - 不会因为参数选择导致计算时间显著增加

### 3.2 与主结果的关联

**建议在论文中建立敏感性分析与主结果的关联**：

> 敏感性分析表明，我们报告的主要结果（基于`alpha0=5.0, kappa=40.0`）在广泛的参数范围内都是稳健的。特别是，基于MAP估计的一致性在所有参数组合下均为1.0000，这进一步支持了我们关于模型能够准确估计粉丝投票份额的结论。

---

## 四、表格和图表的使用

### 4.1 Table S1（敏感性分析结果汇总表）

**位置**：附录或补充材料

**内容**：
- 9行（3个alpha0 × 3个kappa）
- 7列：alpha0, kappa, consistency_map, consistency_mean, accept_rate_median, rel_CI_width_median, margin_median

**关键信息**：
- 所有组合的`consistency_map`均为1.0000
- `consistency_mean`在大多数组合下为1.0000，最低为0.9967
- 相对CI宽度随`kappa`增加而减小
- 接受率稳定在18-21%之间

### 4.2 Figure S1（Consistency vs kappa）

**位置**：附录或补充材料

**内容**：
- X轴：`kappa`（10, 40, 120）
- Y轴：`consistency_mean`
- 多条线：不同`alpha0`（1.0, 5.0, 20.0）

**关键信息**：
- 所有线都在接近1.0的水平
- `kappa`对一致性的影响很小
- `alpha0`对一致性的影响也很小

### 4.3 Figure S2（Relative CI Width vs kappa）

**位置**：附录或补充材料

**内容**：
- X轴：`kappa`（10, 40, 120）
- Y轴：`rel_CI_width_median`
- 多条线：不同`alpha0`（1.0, 5.0, 20.0）

**关键信息**：
- 所有线都随`kappa`增加而下降
- `kappa`对不确定性的影响显著
- `alpha0`对不确定性的影响较小

---

## 五、论文中的表述建议

### 5.1 方法部分（简要提及）

**建议表述**：

> 为了评估模型对关键超参数的稳健性，我们进行了敏感性分析。我们系统地变化了Dirichlet先验的初始浓度参数（`alpha0` ∈ {1.0, 5.0, 20.0}）和动态先验的平滑强度系数（`kappa` ∈ {10.0, 40.0, 120.0}），共9组参数组合。所有敏感性分析使用相同的固定参数（`n_accept=500`, `draw_max=50000`），并评估了模型的一致性、不确定性和计算效率。详细结果见附录Table S1和Figure S1-S2。

### 5.2 结果部分（详细报告）

**建议表述**：

> **敏感性分析结果**：我们评估了模型对关键超参数（`alpha0`和`kappa`）的稳健性。在所有9组参数组合中，基于MAP估计的一致性（`consistency_map`）均为1.0000（100%），表明模型对参数选择不敏感。基于后验均值的一致性（`consistency_mean`）在大多数参数组合下为1.0000，最低为0.9967（300/301周正确）。相对置信区间宽度的中位数随`kappa`增加而减小（从2.3-2.6降至0.7-0.8），而`alpha0`对不确定性的影响较小。接受率在所有参数组合下都稳定在18-21%之间，表明计算效率不受参数选择影响。这些结果表明，模型在广泛的参数范围内都能保持高一致性和合理的计算效率。详细结果见附录Table S1和Figure S1-S2。

### 5.3 结论部分（简要提及）

**建议表述**：

> 敏感性分析表明，我们报告的主要结果在广泛的参数范围内都是稳健的。特别是，基于MAP估计的一致性在所有参数组合下均为1.0000，这进一步支持了我们关于模型能够准确估计粉丝投票份额的结论。

---

## 六、文件清单

### 6.1 可直接使用的文件

1. **`sensitivity_outputs/table_S1.csv`**：敏感性分析结果汇总表（CSV格式）
2. **`sensitivity_outputs/table_S1.md`**：敏感性分析结果汇总表（Markdown格式，便于复制）
3. **`sensitivity_outputs/fig_S1_consistency_vs_kappa.png`**：一致性随kappa变化的图表
4. **`sensitivity_outputs/fig_S2_rel_ci_vs_kappa.png`**：相对CI宽度随kappa变化的图表

### 6.2 参考文件

1. **`敏感性分析总结.md`**：详细的敏感性分析总结（包含所有结果和解释）
2. **`run_sensitivity.py`**：批量运行敏感性分析的脚本
3. **`analyze_sensitivity.py`**：分析敏感性分析结果并生成图表的脚本

---

## 七、关键数据点速查

### 7.1 一致性（Consistency）

- **`consistency_map`**：所有9组参数组合均为**1.0000**（100%）
- **`consistency_mean`**：
  - 大多数组合为**1.0000**（100%）
  - 3组组合为**0.9967**（300/301周正确）

### 7.2 不确定性（Uncertainty）

- **`rel_CI_width_median`**：
  - `kappa=10`：**2.3-2.6**
  - `kappa=40`：**1.2-1.3**
  - `kappa=120`：**0.7-0.8**

### 7.3 计算效率（Efficiency）

- **`accept_rate_median`**：所有组合都在**18-21%**之间

### 7.4 边界清晰度（Margin）

- **`margin_median`**：所有组合都在**0.0055-0.0119**之间

---

**生成时间**：2026年1月30日  
**参考文档**：`敏感性分析总结.md`（详细版本）

